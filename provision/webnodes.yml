---
   - hosts: node*
     sudo: true
     tasks:
       - name: update apt cache
         apt: update_cache=yes
       - name: install useful packages
         apt: name={{ item }} state=present
         with_items:
          - nginx
          - curl
          - vim
          - python-pip
       - name: install requests lib for python
         command: pip install requests

